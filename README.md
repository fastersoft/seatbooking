Clone the respository in your local folder

npm install
npm run dev

http://localhost:3000

Select the Shapes drop down

Select a Shape

use Mouse down in the left area and draw the shape.

To modify click on the shape so that handles are displayed.
